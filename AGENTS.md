# Practice workspace rules

When the user says `問題作って`, `次の問題`, or equivalent without naming a language, use the `daily-go-python-practice` skill and create one next exercise for **both Go and Python**. Read both learning logs first and follow the skill's exercise and review workflow.

When the user explicitly names only Go or Python, create an exercise only for that language.
